{
  "_comment": "TEMPLATE — replace REPLACE_WITH_USERNAME and REPLACE_WITH_REPO_PATH. Then merge the 'mcpServers' block into your Claude Desktop config at ~/Library/Application Support/Claude/claude_desktop_config.json and restart the app. If you'd rather not edit JSON by hand, run scripts/install-mcp-config.sh from the repo root.",
  "mcpServers": {
    "claude101-stt": {
      "command": "/Users/REPLACE_WITH_USERNAME/.claude101-stt/bin/python",
      "args": [
        "REPLACE_WITH_REPO_PATH/mcp-server/server.py"
      ],
      "env": {
        "PARAKEET_MODEL": "mlx-community/parakeet-tdt-0.6b-v3"
      }
    }
  }
}
